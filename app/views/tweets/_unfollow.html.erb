<%= form_for(current_user.active_relationships.find_by(following_id: @user),html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-danger center-block unfollow-button" %>
<% end %>
