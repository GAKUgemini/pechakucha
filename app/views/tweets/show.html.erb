<div class="highlight-blue">
    <div class="container">
        <div class="intro">
            <h1 class="text-center tweetpage">This is the confirmation page of your tweet content</h1>
            <h3 class="text-center tweetpage">&#x1f447;Your tweet content should be displayed below.&#x1f447;</h3>
        <div class="row">
          <div class="col-md-4 col-md-offset-4 display-box">
            <h3 class="text-center">
              <h3 class="text-center">your tweet:<%= @tweet.text %></h3>
            </h3>
          </div>
          <div class="col-md-4 col-md-offset-4 display-box">
            <h3 class="text-center">
              <h4 class="text-center">Comment field</h4>
                <% @tweet.comments.each do |comment| %>
                  <p class="text-center">
                    <strong class="text-center">Comment:</strong>
                      <%= comment.comment %>
                  </p>
                <% end %>
            </h3>
          </div>
          <div class="col-md-4 col-md-offset-4 display-box">
            <h3 class="text-center">
              <h4 class="text-center">Do you want to comment?</h4>
              <%= form_for([@tweet, @tweet.comments.build]) do |f| %>
              <p class="text-center">
                <%= f.label :comment, class:"text-center" %><br>
                <%= f.text_area :comment, class:"text-center" %>
              </p>
              <p class="text-center">
                <%= f.submit %>
              </p>
              <% end %>
            </h3>
          </div>
        </div>
        <a class="btn btn-primary btn-lg btn-circle1 btn-xl center-block" role="button" href="/tweets#index"><i class="glyphicon glyphicon-list"></i></a>
        </div>
        <div class="buttons"></div>
    </div>
</div>
